"""Application package for the MongoDB-backed microservice."""

# Single source of truth for the service version, surfaced in API metadata,
# health responses, and container image tags to keep observability consistent.
__version__ = "0.1.0"
