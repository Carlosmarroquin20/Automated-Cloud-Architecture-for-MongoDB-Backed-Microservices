# Runtime dependencies. Version ranges pin compatible majors to guarantee
# reproducible builds while permitting security patch uptake.
fastapi>=0.111,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.2,<3.0
pymongo>=4.9,<5.0
prometheus-client>=0.20,<1.0
# Distributed tracing. Instrumentation packages follow the 0.x beta line that
# tracks the 1.x API/SDK. Tracing is inert unless enabled through configuration.
opentelemetry-sdk>=1.27,<2.0
opentelemetry-exporter-otlp-proto-http>=1.27,<2.0
opentelemetry-instrumentation-fastapi>=0.48b0,<1.0
opentelemetry-instrumentation-pymongo>=0.48b0,<1.0
