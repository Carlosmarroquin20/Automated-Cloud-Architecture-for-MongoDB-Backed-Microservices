# Development dependencies. Runtime requirements are included by reference to
# keep a single source of truth for production pins.
-r requirements.txt
pytest>=8.2,<10.0
pytest-asyncio>=0.23,<1.0
httpx>=0.27,<1.0
ruff>=0.5,<1.0
mypy>=1.10,<2.0
