# Local full-stack orchestration. `docker compose up --build` starts MongoDB, the
# backend, and the nginx-served frontend on an internal network. The MongoDB URI
# and environment are overridable to target a managed cluster instead.

name: mongodb-microservice

services:
  mongo:
    image: mongo:7
    restart: unless-stopped
    volumes:
      - mongo-data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--quiet", "--eval", "db.adminCommand('ping').ok"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - internal

  backend:
    build:
      context: ./src/backend
    restart: unless-stopped
    environment:
      APP_ENV: ${APP_ENV:-development}
      MONGODB_URI: ${MONGODB_URI:-mongodb://mongo:27017}
      MONGODB_DB_NAME: ${MONGODB_DB_NAME:-microservice}
      MONGODB_PING_ON_STARTUP: "true"
    ports:
      - "8000:8000"
    depends_on:
      mongo:
        condition: service_healthy
    healthcheck:
      test:
        - CMD
        - python3
        - -c
        - import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health/live')
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 15s
    networks:
      - internal

  frontend:
    build:
      context: ./src/frontend
    restart: unless-stopped
    ports:
      - "8080:80"
    depends_on:
      - backend
    networks:
      - internal

volumes:
  mongo-data:

networks:
  internal:
